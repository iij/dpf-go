# 実装計画: [機能名]

**ブランチ**: `[###-feature-name]` | **日付**: [日付] | **仕様**: [リンク]

**入力**: `/specs/[###-feature-name]/spec.md` の機能仕様

**注記**: この雛形は `/speckit-plan` コマンドが埋める。実行の手順はコマンドの定義に記述されている。

## 概要

[機能仕様から抽出した主要な要件 + 調査に基づく技術的な方針]

## 技術的な前提

<!--
  対応が必要: この節の内容を、対象プロジェクトの技術的な詳細に置き換えること。
  ここに示す構成は、検討を進めるための助言であって指示ではない。
-->

**言語 / バージョン**: [例: Go 1.24、Python 3.11、Rust 1.75、または 要明確化]

**主要な依存**: [例: miekg/dns、OpenTelemetry、FastAPI、または 要明確化]

**保存先**: [該当する場合。例: PostgreSQL、ファイル、または 該当なし]

**テスト**: [例: go test、pytest、または 要明確化]

**対象環境**: [例: Linux サーバ、iOS 15+、WASM、または 要明確化]

**プロジェクト種別**: [例: library / cli / web-service / mobile-app / compiler / desktop-app、または 要明確化]

**性能目標**: [領域固有。例: 1000 req/s、10k 行/秒、60 fps、または 要明確化]

**制約**: [領域固有。例: p95 200ms 未満、メモリ 100MB 未満、オフライン動作、または 要明確化]

**規模 / 範囲**: [領域固有。例: 利用者 1 万、100 万行、画面 50 枚、または 要明確化]

## 憲章への適合確認

*ゲート: フェーズ 0 の調査より前に通過しなければならない。フェーズ 1 の設計後に再確認する。*

[憲章ファイルに基づいて決まるゲート]

## プロジェクト構成

### 文書 (本機能)

```text
specs/[###-feature]/
├── plan.md              # 本ファイル (/speckit-plan の出力)
├── research.md          # フェーズ 0 の出力 (/speckit-plan)
├── data-model.md        # フェーズ 1 の出力 (/speckit-plan)
├── quickstart.md        # フェーズ 1 の出力 (/speckit-plan)
├── contracts/           # フェーズ 1 の出力 (/speckit-plan)
└── tasks.md             # フェーズ 2 の出力 (/speckit-tasks。/speckit-plan では作られない)
```

### ソースコード (リポジトリルート)

<!--
  対応が必要: 以下の雛形のツリーを、本機能の具体的な構成に置き換えること。
  使わない選択肢は削除し、選んだ構成を実際のパス (例: apps/admin、packages/something) へ
  展開すること。完成した計画に「選択肢」というラベルを残してはならない。
-->

```text
# [使わない場合は削除] 選択肢 1: 単一プロジェクト (既定)
src/
├── models/
├── services/
├── cli/
└── lib/

tests/
├── contract/
├── integration/
└── unit/

# [使わない場合は削除] 選択肢 2: Web アプリケーション (フロントエンド + バックエンドの構成のとき)
backend/
├── src/
│   ├── models/
│   ├── services/
│   └── api/
└── tests/

frontend/
├── src/
│   ├── components/
│   ├── pages/
│   └── services/
└── tests/

# [使わない場合は削除] 選択肢 3: モバイル + API (iOS / Android を含むとき)
api/
└── [上記 backend と同じ]

ios/ または android/
└── [プラットフォーム固有の構成: 機能モジュール、UI の流れ、プラットフォームのテスト]
```

**構成の決定**: [選んだ構成と、上に挙げた実在のディレクトリへの対応を記述する]

## 複雑さの記録

> **憲章への適合確認で、正当化が必要な逸脱があった場合にのみ記入する**

| 逸脱 | 必要な理由 | より単純な代替案を採らない理由 |
|---|---|---|
| [例: 4 つ目のプロジェクト] | [現時点での必要性] | [3 つでは不足する理由] |
| [例: Repository パターン] | [具体的な問題] | [DB へ直接アクセスするのでは不足する理由] |
